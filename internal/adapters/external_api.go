package adapters

import (
	"context"
	en "github.com/100bench/cryptocurrency_provider.git/internal/entities"
)

func GetRates(ctx context.Context, currency string) (Rates []en.Rate, err error) {
	return nil, nil
}
